@model dotnet_ex.Models.AccountLoginModel

<main class="auth-container">
    <div class="auth-box">
        <h2>Giriş Yap</h2>

        <form asp-action="Login" asp-controller="Account" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>

            <div class="form-group">
                <input asp-for="Email" class="input-field" placeholder="E-posta adresinizi girin" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <div class="form-group password-wrapper">
                <input asp-for="Password" type="password" id="loginPasswordInput" class="input-field" placeholder="Şifrenizi girin" />
                <span class="toggle-password" onclick="toggleLoginPassword()">👁️</span>
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>

            <button type="submit" class="btn-login">Giriş Yap</button>
        </form>

        <div class="login-footer">
            <p>Hesabın yok mu? <a asp-controller="Account" asp-action="Create">Kayıt Ol</a></p>
            <p><a asp-controller="Account" asp-action="ForgotPassword">Şifremi Unuttum</a></p>
        </div>
    </div>
</main>

<script>
    function toggleLoginPassword() {
        const passwordInput = document.getElementById("loginPasswordInput");
        const type = passwordInput.getAttribute("type") === "password" ? "text" : "password";
        passwordInput.setAttribute("type", type);
    }
</script>
